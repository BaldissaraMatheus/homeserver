location ^~ /discover {
    include /config/nginx/proxy.conf;
    resolver 127.0.0.11;
    set $upstream_app overseer;
    set $upstream_port 5055;
    set $upstream_proto http;
    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

}

location ^~ /discover/api {
    include /config/nginx/proxy.conf;
    resolver 127.0.0.11;
    set $upstream_app overseer;
    set $upstream_port 5055;
    set $upstream_proto http;
    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

}
